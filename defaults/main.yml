---
# defaults file for franz

franz_install_dir: /opt/franz
franz_download_dir: "{{ franz_install_dir }}/{{ franz_archive_filename | regex_replace('.tar.gz$', '') }}"

franz_archive_filename: franz-{{ franz_version | regex_replace('^v', '') }}.tar.gz
